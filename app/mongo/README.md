##### Docker configuration for MongoDB Replicas

This separate folder contains the Docker configuration for MongoDB replicas, which is executed alongside the main [docker-compose.yml](../docker-compose.yml) file.
