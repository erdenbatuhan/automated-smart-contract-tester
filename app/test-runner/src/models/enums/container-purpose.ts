enum ContainerPurpose {
  PROJECT_CREATION = 100, // Indicates that the container is being used for creating a new project or updating an existing one.
  TEST_EXECUTION = 101 // Indicates that the container is being used for test execution.
}

export default ContainerPurpose;
